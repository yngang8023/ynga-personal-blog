packages:
  # Keep the Astro blog as the only root workspace package.
  # cloudflare-rag manages its own dependencies and lockfile separately.
  - .

onlyBuiltDependencies:
  - '@parcel/watcher'
  - esbuild
  - sharp
  - swup
  - ttf2woff2
